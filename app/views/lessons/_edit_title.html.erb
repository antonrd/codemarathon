<%= bootstrap_form_for(lesson, layout: :inline) do |f| %>
  <%= f.text_field :title, hide_label: true, class: "long-field input-sm" %>
  <%= f.submit "Update", class: "btn btn-sm btn-primary" %>
<% end %>
